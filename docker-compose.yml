services:
  redwave:
    build: .
    container_name: redwave
    ports:
      - "8000:8000"
    volumes:
      - ./redwave.db:/app/redwave.db
      - ./.env:/app/.env
    env_file:
      - .env
    restart: unless-stopped
